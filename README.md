# Voice Botcast Groupchat

A Flask-based application that enables real-time conversations between AI characters using voice synthesis.

## Requirements

- Python 3.9.2
- Install dependencies:

```
pip install -r requirements.txt
```
# botcast
